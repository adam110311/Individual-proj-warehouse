<Window x:Class="Warehouse.Desktop.Views.OrderListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="IT Warehouse - Orders"
        Width="850" Height="450"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="130"/>
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Column="0"
                      ItemsSource="{Binding Orders}"
                      SelectedItem="{Binding SelectedOrder}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      BorderThickness="0"
                      Margin="8"
                      MouseDoubleClick="DataGrid_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID"        Binding="{Binding O_ID}"        Width="50"/>
                    <DataGridTextColumn Header="Type"      Binding="{Binding OrderType}"    Width="90"/>
                    <DataGridTextColumn Header="Reference" Binding="{Binding Reference}"    Width="*"/>
                    <DataGridTextColumn Header="Created"   Binding="{Binding CreatedDate, StringFormat=yyyy-MM-dd}" Width="100"/>
                    <DataGridTextColumn Header="Status"    Binding="{Binding Status}"       Width="100"/>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Column="1" Margin="8" VerticalAlignment="Top">
                <Button Content="Add"      Command="{Binding AddOrderCommand}"      Padding="8,6" Margin="0,0,0,6"/>
                <Button Content="Delete"   Command="{Binding DeleteOrderCommand}"   Padding="8,6" Margin="0,0,0,6"/>
                <Button Content="Complete" Command="{Binding CompleteOrderCommand}" Padding="8,6" Margin="0,0,0,6"/>
            </StackPanel>
        </Grid>

        <StatusBar Grid.Row="1">
            <TextBlock Text="{Binding StatusMessage}" FontSize="12"/>
        </StatusBar>
    </Grid>
</Window>
