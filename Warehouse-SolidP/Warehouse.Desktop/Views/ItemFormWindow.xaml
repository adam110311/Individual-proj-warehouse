<Window x:Class="Warehouse.Desktop.Views.ItemFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Item"
        Width="440" Height="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Name" VerticalAlignment="Center" Margin="0,0,0,8"/>
        <TextBox   Grid.Row="0" Grid.Column="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Padding="4" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Brand" VerticalAlignment="Center" Margin="0,0,0,8"/>
        <TextBox   Grid.Row="1" Grid.Column="1" Text="{Binding Brand, UpdateSourceTrigger=PropertyChanged}" Padding="4" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Serial number" VerticalAlignment="Center" Margin="0,0,0,8"/>
        <TextBox   Grid.Row="2" Grid.Column="1" Text="{Binding SerialNumber, UpdateSourceTrigger=PropertyChanged}" Padding="4" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Category" VerticalAlignment="Center" Margin="0,0,0,8"/>
        <ComboBox  Grid.Row="3" Grid.Column="1"
                   ItemsSource="{Binding Categories}"
                   SelectedItem="{Binding SelectedCategory}"
                   Padding="4"
                   Margin="0,0,0,8"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="Status" VerticalAlignment="Center" Margin="0,0,0,8"/>
        <ComboBox  Grid.Row="4" Grid.Column="1"
                   ItemsSource="{Binding StatusOptions}"
                   SelectedItem="{Binding Status}"
                   Padding="4"
                   Margin="0,0,0,8"/>

        <TextBlock Grid.Row="5" Grid.Column="0" Text="Description" VerticalAlignment="Top" Margin="0,0,0,8"/>
        <TextBox   Grid.Row="5" Grid.Column="1"
                   Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                   AcceptsReturn="True"
                   TextWrapping="Wrap"
                   Height="60"
                   Padding="4"
                   Margin="0,0,0,8"/>

        <TextBlock Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2"
                   Text="{Binding ErrorMessage}"
                   Foreground="Red"
                   FontSize="12"
                   TextWrapping="Wrap"
                   Margin="0,0,0,8"/>

        <StackPanel Grid.Row="8" Grid.Column="0" Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Cancel" Padding="16,6" Margin="0,0,8,0" IsCancel="True"/>
            <Button Content="Save"   Padding="16,6" Command="{Binding SaveCommand}"/>
        </StackPanel>
    </Grid>
</Window>
